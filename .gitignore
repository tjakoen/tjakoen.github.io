node_modules
dist/
screenshots/
test-results/
*.log
.DS_Store

# graphify's generated code+doc graph (the mindmap's source) — regenerable, host-local
graphify-out/
playwright-report/

# desk-audit's persistent browser profile + graded reports (tools/desk-audit.ts) — host-local,
# holds the ~350MB cached model weights
.cache/

# Nimbalyst editor state + local plans/session notes — private working material, not site content
.nimbalyst/
nimbalyst-local/

# S0 pilot: third-party agent-skills, mounted not committed (plans/skills-runtime.md S2 owns the final call)
.agents/
.claude/skills/
skills-lock.json
